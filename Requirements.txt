# ── Telegram bot framework ────────────────────────────────────────────────────
python-telegram-bot==21.5

# ── Whisper AI (local speech-to-text) ────────────────────────────────────────
openai-whisper==20231117

# ── NumPy (Whisper dependency — keep <2 for whisper compat) ──────────────────
numpy>=1.24,<2.0

# ── Translation backends (install what you need) ──────────────────────────────
# Option A — OpenAI ChatGPT (set OPENAI_API_KEY in .env)
openai>=1.35,<2.0

# Option B — DeepL (set DEEPL_API_KEY in .env)
# deepl==1.18.0

# Option C — LibreTranslate HTTP client (set LIBRETRANSLATE_URL in .env)
httpx>=0.27,<1.0

# Option D — Argos Translate (offline fallback, no key needed)
argostranslate==1.9.6

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
